import { createSlice } from "@reduxjs/toolkit";

const cartSlice=createSlice({
    name:'cart',
    initialState:{
        items:[] //initialising an empty cart 
    },
    reducers:{ //reducer is that function which updates the value in the store

    }
})